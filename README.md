# STAR Website

A modern school/college website project built with Vite, React, and TypeScript.

## Getting started

1. Install dependencies with `bun install`.
2. Start the development server with `bun run dev`.
3. Open the local preview URL shown in the terminal.
